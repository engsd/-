# 问卷星速填 Android App

一个用于自动填写问卷星问卷的Android应用程序。

## 功能特性

- 🔗 **问卷链接输入** - 支持直接输入问卷URL
- 📷 **二维码扫描** - 支持扫描问卷二维码获取链接
- 🖼️ **图片识别** - 支持从相册选择二维码图片识别
- ⚡ **自动配置** - 自动解析问卷结构，智能配置题目
- 📝 **题目配置** - 支持多种题型配置（单选、多选、填空、下拉、矩阵、量表等）
- 🎯 **概率控制** - 支持自定义各选项的选择概率
- 🚀 **批量执行** - 支持设置目标份数和执行间隔
- 📊 **进度显示** - 实时显示执行进度和成功/失败统计
- 📋 **运行日志** - 详细的运行日志记录

## 支持的题型

| 题型 | 说明 |
|------|------|
| 单选题 | 支持随机选择或自定义权重 |
| 多选题 | 支持设置各选项的选中概率 |
| 填空题 | 支持多个预设答案随机选择 |
| 下拉题 | 支持随机选择或自定义权重 |
| 矩阵题 | 支持多行多列的矩阵单选 |
| 量表题 | 支持评分量表题 |
| 位置题 | 支持位置/地址填写 |

## 在 Android Studio 中打开项目

### 方法一：直接打开

1. 打开 Android Studio
2. 选择 `File` -> `Open`
3. 选择 `app` 文件夹
4. 等待 Gradle 同步完成

### 方法二：导入项目

1. 打开 Android Studio
2. 选择 `File` -> `New` -> `Import Project`
3. 选择 `app` 文件夹
4. 等待 Gradle 同步完成

## 构建项目

### 使用 Android Studio

1. 打开项目后，等待 Gradle 同步完成
2. 点击 `Build` -> `Make Project` 或按 `Ctrl+F9`
3. 构建完成后，点击 `Run` -> `Run 'app'` 或按 `Shift+F10`

### 使用命令行

```bash
# Windows
gradlew.bat assembleDebug

# Linux/Mac
./gradlew assembleDebug
```

生成的 APK 文件位于 `app/build/outputs/apk/debug/` 目录下。

## 项目结构

```
app/
├── build.gradle.kts          # 应用构建配置
├── settings.gradle.kts       # 项目设置
├── gradle.properties         # Gradle属性
├── proguard-rules.pro        # ProGuard混淆规则
├── gradlew / gradlew.bat     # Gradle Wrapper脚本
├── gradle/
│   └── wrapper/
│       └── gradle-wrapper.properties
└── src/
    └── main/
        ├── AndroidManifest.xml
        ├── java/com/wjx/filler/
        │   ├── MainActivity.kt           # 主界面
        │   ├── WebViewActivity.kt        # WebView页面
        │   ├── adapter/
        │   │   └── QuestionAdapter.kt    # 题目列表适配器
        │   ├── core/
        │   │   ├── SurveyExecutor.kt     # 问卷执行器
        │   │   ├── SurveyFiller.kt       # 问卷填写引擎
        │   │   └── SurveyParser.kt       # 问卷解析器
        │   └── model/
        │       ├── QuestionEntry.kt      # 题目配置模型
        │       └── SurveyConfig.kt       # 问卷配置模型
        └── res/
            ├── drawable/                  # 图标资源
            ├── layout/                    # 布局文件
            ├── menu/                      # 菜单资源
            ├── mipmap-*/                  # 应用图标
            └── values/                    # 字符串、颜色、尺寸等资源
```

## 技术栈

- **语言**: Kotlin
- **最低SDK**: Android 7.0 (API 24)
- **目标SDK**: Android 14 (API 34)
- **UI框架**: Material Design 3
- **网络请求**: OkHttp
- **HTML解析**: Jsoup
- **二维码**: ZXing
- **异步处理**: Kotlin Coroutines

## 依赖库

- AndroidX Core KTX
- AndroidX AppCompat
- Material Components
- ConstraintLayout
- Lifecycle Components
- ZXing (二维码扫描)
- Gson (JSON解析)
- OkHttp (网络请求)
- Jsoup (HTML解析)
- Kotlin Coroutines

## 权限说明

| 权限 | 用途 |
|------|------|
| INTERNET | 访问网络加载问卷 |
| ACCESS_NETWORK_STATE | 检查网络状态 |
| CAMERA | 扫描二维码 |
| READ_EXTERNAL_STORAGE | 读取相册中的二维码图片 |
| VIBRATE | 扫描成功时震动反馈 |

## 注意事项

⚠️ **免责声明**: 本工具仅供学习交流使用，请勿用于任何违法违规用途。使用本工具所产生的一切后果由使用者自行承担。

## 许可证

MIT License